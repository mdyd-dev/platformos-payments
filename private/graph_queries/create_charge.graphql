mutation create_charge(
  $credit_card_id: String!
  $currency: String!
  $description: String
  $statement_descriptor: String
  $capture: String = "true"
  $customer_id: String
  $external_customer_id: String
  $destination: String
  $transfer_group: String
  $amount_cents: String
  $application_fee_cents: String
) {
  charge: customization_create(
    customization: {
      properties: [
        { name: "credit_card_id", value: $credit_card_id }
        { name: "currency", value: $currency }
        { name: "description", value: $description }
        { name: "statement_descriptor", value: $statement_descriptor }
        { name: "capture", value: $capture }
        { name: "customer_id", value: $customer_id }
        { name: "external_customer_id", value: $external_customer_id }
        { name: "destination", value: $destination }
        { name: "transfer_group", value: $transfer_group }
        { name: "amount_cents", value: $amount_cents }
        { name: "application_fee_cents", value: $application_fee_cents }
      ]
    }
    form_configuration_name: "modules/stripe_payments/create_charge_form"
  ) {
    id
    properties
  }
}
