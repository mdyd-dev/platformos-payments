mutation create_customer_in_stripe($data: HashObject) {
  api_call_send(
    data: $data
    template: { name: "modules/payments/create_customer_in_stripe" }
  ) {
    response{ status body }
    errors {
      message
    }
  }
}
