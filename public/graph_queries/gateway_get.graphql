mutation gateway_get($data: HashObject, $template: String!) {
  api_call_send(
    data: $data
    template: { name: $template }
  ) {
    response{ status body }
    errors {
      message
    }
  }
}
