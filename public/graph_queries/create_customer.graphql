mutation create_customer(
  $customer: CustomizationInputType!
) {
  customer: customization_create(
    customization: $customer
    form_configuration_name: "modules/payments/create_customer_form"
  ) {
    id
    properties
  }
}
