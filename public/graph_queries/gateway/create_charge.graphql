mutation gateway_create_charge($data: HashObject, $gateway_template: String!) {
  api_call_send(
    data: $data
    template: { name: $gateway_template }
  ) {
    response{ status body }
    errors {
      message
    }
  }
}
