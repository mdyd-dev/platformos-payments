mutation gateway_create($gateway_template: String!, $data: HashObject)  {
  api_call_send(
    data: $data
    template: { name: $gateway_template }
  ) {
    response{ status body }
    errors {
      message
    }
  }
}
