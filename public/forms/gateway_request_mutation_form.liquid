---
name: gateway_request_mutation_form
resource: modules/payments/gateway_request
resource_owner: anyone
authorization_policies: ''
fields:
  properties:
    request_type:
    gateway:
    config:
    data:
    default_data:
      property_options:
        virtual: true
    response:
    request_errors:
callback_actions: >
default_payload: >
  {% if form %}
    {%- parse_json config %}{{ params.properties_attributes.config }}{% endparse_json %}
    {%- parse_json data %}{{ params.properties_attributes.data }}{% endparse_json %}
    {%- include "modules/payments/forms/gateway_request_default_payload", params: params, config: config, data: data -%}
  {% endif %}
---
