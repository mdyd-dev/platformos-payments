mutation create_card_in_stripe($data: HashObject) {
  api_call_send(
    data: $data
    template: { name: "modules/stripe_payments/create_card_in_stripe" }
  ) {
    response{ status body }
    errors {
      message
    }
  }
}
